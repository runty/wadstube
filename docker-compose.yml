services:
  tube:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY}
      - DATA_DIR=/app/data
      - PORT=3000
      - MAX_VIDEOS=${MAX_VIDEOS:-15}
    restart: unless-stopped
    mem_limit: 512m
